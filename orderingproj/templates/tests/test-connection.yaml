apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orderingproj.fullname" . }}-test-connection"
  labels:
    {{- include "orderingproj.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orderingproj.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
